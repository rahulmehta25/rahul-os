# ElevenLabs Conversational AI agent id.
# Get one from https://elevenlabs.io/app/conversational-ai.
VITE_ELEVENLABS_AGENT_ID=agent_5401kpfsmt25ejts36bh2s3rjcye

# PostHog (optional — falls back to shared portfolio key if unset).
VITE_POSTHOG_KEY=phc_MdeB4wNRGn9gckJkl0ZZDh1XEIlvsfWBa23qv9lty5Z
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Visitor leaderboard API (where scripts/serve-visitors.ts listens).
VITE_API_BASE_URL=http://localhost:8787

# Server-side DATABASE_URL for scripts/serve-visitors.ts and scripts/seed-visitors.ts.
#   1. Run scripts/db-proxy.sh in another terminal (brew install cloud-sql-proxy).
#   2. gcloud secrets versions access latest --secret=portfolio-pg-postgres-password \
#        --project=osmoti-auth
#   3. Replace CHANGEME with that password.
DATABASE_URL=postgresql://postgres:CHANGEME@127.0.0.1:5432/rahulos?sslmode=disable
